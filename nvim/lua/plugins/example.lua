-- since this is just an example spec, don't actually load anything here and return an empty spec
-- stylua: ignore
if true then return {} end -- This line disables the entire file

-- All configurations have been moved to their respective plugin files or deemed unnecessary.
-- This file is now empty and disabled.
return {
  -- Content removed/migrated
}
